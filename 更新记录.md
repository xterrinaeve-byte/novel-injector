# Novel Injector 完整更新记录

> 整理截止时间：2026-09-07（Asia/Shanghai）
> 覆盖范围：2026-05-13 至 2026-09-07
> 阅读说明：同一天内属于同一功能的多次调整已合并为一句话，以用户最终能看到和使用的效果为准，不再重复记录试改、回滚和内部验收过程。

---

## 2026-05-13

- 小说处理：建立 TXT 上传、编码识别、按大小分段、处理进度展示和 AI 清洗压缩的第一套完整流程。
- API 请求：加入接口、Key、模型、流式输出、并发限流和基础错误提示，提升长篇连续清洗的稳定性。
- 剧情与角色：初步建立主线、支线、关键转折和角色资料体系，并支持基础查看、生成、添加与删除。
- 阶段与注入：支持自动或手动划分阶段、分配节点、按阶段向量化，并可设置召回数量、相似度和注入位置。

## 2026-05-14

- 清洗可靠性：加入暂停、跳过当前段、请求超时、格式异常恢复和递增等待重试。
- 剧情编辑：支持新增、编辑和删除节点，并建立主线、转折与支线之间的关联和 AI 修补入口。
- 角色管理：将人设扩展为身份、外貌、性格和关系四类字段，同时支持按阶段和分类批量开关。
- 向量与偏差：增加向量阶段批量选择和“当前偏差”分析，并收紧主线、支线和关键转折的清洗判定规则。

## 2026-05-15

- 插件与注入：增加插件总开关，并允许角色、阶段剧情和向量召回分别设置注入位置、深度与消息角色。
- 小说库与维护：加入阶段抽屉、小说快照、轻量导入导出、向量清理和插件数据维护入口。
- 数据整理：强化角色去重、阶段映射和时间线定位，让剧情节点与角色首次登场能跟随阶段管理。
- 使用说明：重写 README，形成安装、清洗、剧情管理、向量化和注入的基础教程。

## 2026-05-16

- 清洗续跑：增加每分钟请求上限、单段重洗、分段元数据保存和跨分段摘要参考，失败后可从已完成内容恢复剧情与角色。
- 剧情操作：支持剧情节点拖拽排序并保存调整后的顺序。
- 角色与阶段：区分原始人设和 AI 更新入口，并支持单角色、批量人设更新以及阶段概括补全或重建。

## 2026-05-17

- 演绎规则：确立“原著是参考而非强制剧本”，要求 AI 尊重用户改变、推演连锁反应并遵守角色信息边界。
- 阶段提示词：每个阶段都可单独编辑、启用或恢复演绎提示词。
- 阶段注入：已向量阶段使用语义召回，未向量阶段直接注入剧情资料，关闭阶段则完全跳过。

## 2026-05-18

- 全局提示词：增加可用于所有模型调用的统一全局约束。
- 小说快照：支持快照重命名和覆盖更新，便于长期维护同一本小说。
- 清洗质量：重写清洗提示词并加入缺失元数据重试和裸 JSON 抢救，要求压缩正文完整覆盖剧情、关系、时间、地点、因果与转折。

## 2026-05-20

- 未向量注入：可在“剧情节点”和“压缩原文”之间选择，分别兼顾 Token 消耗和原文细节。
- 压缩规则：要求先完整输出压缩正文再输出结构化数据，并限制过度缩写，避免向量检索缺少关键信息。

## 2026-05-21

- 世界设定：新增可分类生成、编辑、启用和注入的世界观资料模块。
- 清洗规范：进一步要求压缩正文覆盖每个时间、地点和结构化节点，避免提前输出元数据造成正文缺失。
- 阶段说明：在界面中明确区分向量召回、剧情节点直注和压缩原文直注。

## 2026-05-22

- 向量诊断：增加 IndexedDB 向量块、阶段分布和实际存储状态的检查入口。
- 世界设定界面：优化批量生成操作和分类布局。
- 角色提取：要求模型客观区分声明、行为和旁白事实，减少把猜测动机写入固定人设。

## 2026-05-23

- 向量存储与备份：向量改为二进制保存，ZIP 导入导出可携带向量并继续兼容旧 JSON。
- 重数据管理：将正文和分段等大型数据拆到服务端文件，并加入缺失恢复、删除提示和小说指纹校验。
- 向量设置：增加独立限速和“暂时关闭召回但保留向量”的开关。
- 角色字段：增加性别、分类和首次登场阶段，并支持在新增或编辑时直接设置。

## 2026-05-24

- 文风分析：支持从原著采样或手动粘贴范文，生成可编辑、可注入的文风执行指南。
- 穿书模式：建立阶段、当前节点、完成状态、推进提示词、持续提示词和节点归档的基础流程。
- 后文推演：可生成三条不同方向的剧情建议，并加强支线、伏笔和阶段之间的联动。
- 外观：完成草莓抹茶风格的初版配色与状态样式统一。

## 2026-05-25

- 穿书提示词：将节点切换时的一次性推进提示与每轮持续提示分开，并增加第一阶段开场提示。
- 用户主导：节点时间地点只作为锚点，用户改变剧情后由 AI 自适应推演，阶段完成和关键推进必须由用户确认。
- 界面：优化穿书开关、提示词面板、状态文案和主题配色。

## 2026-05-26

- 穿书显示：新增正文状态栏和可拖动“小票”弹窗两种方式，可切换阶段节点、查看进度、暂停和推演后文。
- 节点操作：弹窗可查看剧情类型、伏笔和完成状态，并支持上一节点、下一节点与当前节点高亮。
- 推演与文档：后文建议改为具体可执行的动作和场景，同时补充世界设定、文风、注入和穿书教程。

## 2026-05-27

- 界面修复：修正插件与穿书状态文字、标题布局以及移动端拖动弹窗时页面跟随滚动的问题。
- 内容提示：为空的阶段概括增加占位，避免用户把空白误认为加载失败。
- 发布整理：备份说明加入文风数据，并移除误打包的临时样式文件。

## 2026-05-28

- 发布整理：当天仅重新打包和复制发布，核心功能、提示词与界面没有变化。

## 2026-05-29

- 向量完整性：只有全部向量块写入成功才标记阶段完成，并可诊断和补全缺失块。
- 召回质量：支持提取指定消息标签、结合最近多条对话并按时间加权，也可为部分模型添加查询前缀。
- 穿书召回：可用隐藏归纳词和当前节点的标题、时间、地点辅助下一轮检索，并修正失败后仍显示已完成的问题。

## 2026-05-30

- 使用说明：仅更新 README，补充向量诊断、缺失补全、消息提取、加权召回和阶段批量操作说明。

## 2026-06-03

- 写作提示词：扩充禁词与禁用句式，要求用具体动作、身体反应和场景细节替代抽象标签及套路化表达。

## 2026-06-06

- 项目发布：插件正式导入 GitHub，建立可追踪的云端版本历史并整理公开安装与使用说明。
- 数据架构：将清洗正文和分段元数据等重数据拆到服务端文件，并增强旧文件名兼容、缺失恢复、阶段重建和向量对账。
- 穿书与角色：增加归纳、沉浸边界和更尊重当前对话事实及用户主动权的角色演绎规则。
- 代码结构：拆出拼音、存储、向量和 ZIP 等工具模块，为后续模块化维护做准备。

## 2026-06-07

- 后文建议：推演结果固定为情感、张力和伏笔三个方向，点击即可填入 SillyTavern 输入框作为下一步行动。
- 用户代入：支持角色别名合并、选择代入角色和称呼，并按聊天保存相关配置。
- 剧情关联：支线可选择关联主线或转折并指定插入位置，README 同步更新完整操作说明。

## 2026-06-08

- 主题编辑：新增完整颜色编辑器、预设管理、夜间主题和跟随美化、去边框、去卡片、状态栏跟随等开关。
- 提示词：新增可与头部提示词分开配置的全局尾部提示词。
- 状态反馈：修正向量不完整时的成功状态，并统一各类开关的文字和主题色反馈。

## 2026-06-09

- 用户边界：原著中需要用户参与的台词、行为和选择必须等待用户输入，AI 不得擅自替用户完成。
- 穿书召回：新增轻量化裁切，在不改动原文和向量的前提下过滤明显属于未来剧情的召回内容。
- 界面联动：插件总开关可暂停并恢复穿书状态，无边框主题仍保留必要控件的弱边框。

## 2026-06-11

- 酒馆预设：全局提示词可选择跟随 SillyTavern 当前预设，并安全处理消息顺序、变量、宏和后台任务身份。
- API 兼容：改进流式响应解析，清洗已有进度时可安全选择重洗或续跑并阻止重数据缺失下的危险覆盖。
- 主题细节：无边框模式下当前小说卡片仍会使用主题主色标识。

## 2026-06-12

- AI 实时人设：批量和单角色更新共用统一流程，可识别截断、空响应和 JSON 异常并自动重试，失败时显示具体角色和原因。
- 数据保护：实时人设写入独立字段，不覆盖清洗得到的原始人设，同时优化单角色更新按钮和说明。

## 2026-06-14

- 剧情稳定性：为节点建立稳定 ID，并在清洗重建、阶段重建和编辑后尽量保留用户排序与阶段归属。
- 剧情浏览：主线、支线和转折统一使用同一故事顺序，节点跳转时提供更明显的定位与高亮。
- 向量输出：召回结果在语义筛选后按原故事顺序注入，避免片段顺序打乱叙事。

## 2026-06-18

- 清洗界面：修复部分完成后重洗与续跑按钮错位，并清楚显示已完成、失败数量和窄屏操作方向。

## 2026-06-21

- 角色自动休眠：切换阶段时可自动关闭本阶段未出现的非主角角色，同时保护主角和用户代入角色。
- 当前偏差：偏差分析结果可保存并继续注入，拥有独立设置、提示词和折叠显示。

## 2026-06-22

- 偏差自动更新：可按对话楼层间隔自动分析或限制手动分析范围，并针对无资料、无召回等情况显示明确提示和加载状态。
- 状态与样式：穿书暂停状态在状态栏、弹窗和设置间同步，并统一复选框、按钮和主题控件外观。

## 2026-06-23

- 偏差增量分析：按聊天保存覆盖楼层，只处理新增区间并可重试指定范围，避免不同聊天共用同一结果。
- 人设任务：AI 实时人设批量更新支持取消，可中止请求、重试等待和后续角色任务并显示完整统计。

## 2026-06-25

- 偏差编辑：将结果拆为已改变事实、当前约束和仍有效原著事实三个可编辑区，并支持增量合并和楼层进度显示。
- 用户代入：增加替换模式与扮演模式，并可预览、编辑实际注入的身份边界提示。
- 主题图标：主题编辑器增加默认图标与自定义图标风格切换。

## 2026-06-26

- 穿书进度：自动校正当前节点和可推进边界，支线可依据关联主线或转折推导阶段归属。
- 召回输入：统一 XML 标签块、消息角色和可计数消息的提取规则。
- 用户代入：替换、扮演和关闭状态拥有独立提示词，可分别编辑和保存。
- 角色显示：进一步区分原始人设与 AI 实时人设，并在关闭注入时显示明确占位。

## 2026-06-27

- 人设准确性：更新前确认目标角色近期确实出现，并校验模型返回人物，避免凭空生成或批量串角色。
- 偏差楼层：统一使用真实聊天楼层，修正手动、自动和范围重试的区间提示，并可补跑漏掉的范围。
- 穿书节点：点击节点行可展开摘要，勾选区域继续只负责完成状态。
- 用户代入：区分姓名别名与身份称谓，替换仅作用于注入文本和最终提示并保护标签内容。

## 2026-07-01

- 人设隔离：AI 实时人设改为按聊天保存，小说快照不再把一个聊天的临时状态带到另一个聊天。
- 代入语义：替换模式不再把所选角色当作独立 NPC，扮演模式则保留其人设作为用户角色基础，并可独立编辑 AI 人设字段。

## 2026-07-04

- 并发设置：清洗类请求和向量请求拥有独立并发上限，并继续与每分钟限速共同生效。
- 向量效率：阶段向量化和缺失补全可并行处理多个文本块，配置区也可随新增输入项自动换行。

## 2026-07-08

- 顶栏入口：可隐藏或恢复 SillyTavern 顶栏书本图标，并保护图标字体不受字体美化插件影响。
- MOBI 导入：支持常见未加密 MOBI 正文解析，对 DRM、特殊压缩和损坏文件给出明确转换建议。

## 2026-07-09

- 顶栏修复：恢复被隐藏的插件图标时会清理旧隐藏样式，点击后可正常重新打开抽屉。

## 2026-07-11

- 世界设定生成：检测到输出被 Token 上限截断时会自动重试并显示状态。
- 清洗续跑：调整分段大小时尽量保留已完成内容，无法安全恢复时取消重分段以避免丢失进度。
- 模块与向量：拆分更多可验证工具模块，并在重分段后重置受影响向量状态、重新映射来源分段。

## 2026-07-12

- 代码模块化：将 API、清洗、提示词、存储、剧情、穿书、UI、向量和世界设定整理为独立业务模块。
- 清洗执行：清洗、阶段概括和角色人设共用普通生成并发上限，并统一暂停、跳过、重试和进度统计。
- 数据保护：重建剧情和角色时保留稳定 ID、用户排序、阶段归属与手动编辑，避免复活已删除内容。
- 界面与切换：稳定插件抽屉滚动、保留向量阶段选择、支持世界设定分类管理，并修复切换小说后旧数据残留。

## 2026-07-13

- 长列表操作：穿书状态栏和小票的阶段、节点列表拥有独立滚动区域，移动端不再撑出屏幕。
- 节点交互：点击节点行负责切换当前节点，右侧勾选只改变归档状态，两种操作不再混淆。
- 进度同步：跳到同阶段后续节点时自动补全此前进度，并同步高亮、说明、统计和滚动位置。

## 2026-07-15

- 自动保存：可选择自动创建或覆盖当前小说快照，内容变化防抖保存，失败不会打断清洗和操作。
- 偏差状态：聊天楼层变化后自动更新倒计时提示。
- 向量诊断：改为可滚动的独立窗口，移动端能完整查看长报告。

## 2026-07-17

- 剧情关联：关键转折和支线使用统一的可搜索选择器确定关联节点与位置，移动后同步更新顺序和阶段映射。
- 状态保留：向量阶段选择、世界设定展开状态和偏差倒计时在重新渲染后继续保持。
- 节点感知召回：结合当前穿书节点距离调整向量结果权重，让附近剧情更容易被召回。
- 阶段注入界面：根据向量状态和用户开关正确显示向量控制或未向量注入方式。

## 2026-07-18

- 用户代入：配置统一按聊天持久化并兼容旧数据，切换后同步刷新角色注入、人设、提示词与预览。
- 穿书状态：切换当前节点时同步保存聊天中的真实节点索引，并规范旧快照中的无效模式值。
- 提示词界面：全局提示词面板固定显示在对应按钮和标题下方，不再漂移到其他设置区域。

## 2026-07-19

- 分支事实：将偏差结果升级为带稳定 ID、类型、楼层和状态的结构化事实，可单独编辑并记录新增、更新和移除历史。
- 界面优化：事实正文默认折叠，AI 人设关闭占位和剧情页粘性工具栏的显示更加一致。
- 指纹兼容：没有 Web Crypto 时使用纯 JavaScript SHA-256 回退，仍可计算小说指纹和上传文件。

## 2026-07-20

- 节点预览：将“查看其他节点”和“真正推进剧情”分开，浏览标题与正文不会改变当前进度，重新打开时会回到真实节点。

## 2026-07-21

- 事实楼层：偏差事实优先记录首次在聊天正文中成立的来源楼层，后续被改写时另行显示更新楼层。
- 向量批处理：Embedding 可按设置一次提交多个文本块，正常向量化和缺失补全都能显著减少请求次数。
- 错误统计：整批 API 失败和单块写入失败分别记录，进度不会因局部错误而失真。

## 2026-07-22

- 向量界面：批量大小选择器更紧凑，移动端提示图标和说明文字保持同一行对齐。
- 备份隐私：ZIP 不再导出 API 地址、模型或 Key，导入时保留本机配置并自动摘要化旧版明文向量来源。
- 向量兼容：来源指纹只用于诊断，实际按向量维度决定能否继续召回，切换模型不会无故清空旧向量。
- 阶段选择：首次打开默认排除已向量阶段，并用提示标记异源或混合维度但仍可用的数据。

## 2026-07-24

- 旧聊天迁移：兼容多种来源楼层标记和历史异常数据，恢复事实真实来源并只迁移一次。
- 向量开关：只有用户明确关闭后才停用召回，旧设置残留不会再意外关闭向量。
- 原著召回：当前剧情与原著前情分开保留名额，结合聊天和当前节点检索，隔离未来阶段并减少重复请求和数据库读取。
- 内容边界：去除跨阶段重复原文，在同一分段内继续裁掉当前节点之后的内容，穿书直注和偏差分析都只使用允许范围。

## 2026-07-28

- 穿书暂停：暂停只停止剧情直注和开场、推进、持续提示，沉浸边界与向量召回继续按各自开关工作，待发推进恢复后仍会生效。
- 角色初登场：乱序清洗、续跑和单段重洗时始终由更早分段的人设作为基础，同时保留用户编辑、开关、别名和删除状态。
- 回归验证：增加乱序合并、续跑、重洗和删除不复活等专项检查，并完成真实小说分段测试。

## 2026-07-29

- 分页与性能：阶段和角色列表使用轻量分页并只渲染当前页，大量阶段下仍能快速跳转和切换分类。
- 批量阶段操作：全部开启或关闭只执行一次保存与刷新，只有用户单独开启阶段才触发自动休眠和 AI 人设更新。
- 直接注入：未向量阶段增加默认 32000 Token 单轮上限，空间不足时先使用阶段概括，再跳过超限内容并给出说明。
- 向量与界面：增加按小说和阶段缓存，统一紧凑按钮与分页控件样式，并加入 1000 阶段专项回归检查。

## 2026-07-30

- 界面细节：校正“修补关联”图标对齐，并让分页栏只在确实需要翻页时显示。
- 分页记忆：阶段列表按聊天记住当前页，刷新和切换聊天后会恢复各自位置并自动校正越界页码。

## 2026-08-01

- 阶段索引：将节点阶段归属保存为独立持久化索引，加载时以小说正文和剧情节点文件为事实来源并兼容旧快照。
- 统一分页：剧情、角色、阶段、阶段划分、穿书列表、小票和小说库统一使用完整左右分页栏并只渲染当前页或当前展开内容。
- 保存可靠性：正文、分段和阶段文件按目标串行写入，自动保存和快照上传绑定原小说，避免慢请求或切换小说造成互相覆盖。
- 数据保护与测试：修复空剧情覆盖阶段、旧映射污染新小说等问题，并增加大规模分页、乱序写入、跨小说保存和 ESM 解析检查。

## 2026-08-02

- 穿书阶段识别：旧节点即使缺少有效阶段编号也会从阶段映射恢复真实归属，避免误进第 0 阶段以及剧情直注和 Token 统计失效。
- 直接注入提示：阶段页会按本轮真实注入计划即时显示 Token，用不到普通直注时自动隐藏，穿书模式只显示本轮用量，普通模式同时显示上限和必要异常说明。
- 剧情分页记忆：时间线、主线、支线和关键转折会按聊天保存各自页码，刷新或切换聊天后恢复原位置。
- 分页箭头：将不稳定的 CSS 细线箭头替换为更清晰的细矢量图标，并与页面其他次级图标保持一致。
- 穿书状态栏：首次展开时立即生成节点卡并在动画后校正位置，修复 Edge 中必须重选阶段才能显示的问题。

## 2026-08-06

- 预览节点记忆：浏览其他节点后刷新页面或切换聊天，会恢复退出前预览的节点（含已归档节点），并在真正产生新消息前保持原位，不再被自动拉回当前节点。
- 状态栏数据同步：节点归档状态变化后卡片点击始终读取最新数据，勾选完成与取消归档会即时同步到节点本身。
- 毛玻璃显示修复：修复 Edge/Chromium 毛玻璃主题下状态栏轮播区内容不绘制的问题，通过图层隔离与展开动画后的强制重绘保证始终可见。
- 状态栏重挂载：刷新或新楼层重挂载后状态栏默认闭合，需要时再手动展开。
- 代入角色搜索：用户代入的角色选择改为可搜索选择器，输入角色姓名中的一个字即可过滤干扰项，选项右侧同排显示初次登场阶段。

---

## 2026-08-07

- 注入节点同步修复：修复状态栏选中节点与后台实际注入节点不一致的问题。此前轮播卡片点击、弹窗上下箭头只改预览下标，勾选归档也不推进后台节点，导致界面已切到下一节点、后台仍在发送上一节点（未向量阶段尤其明显）。现在后台注入节点由归档进度推导——取当前阶段内第一个未归档、未锁定的节点，勾选归档后自动前移，翻看预览不再影响注入。
- 归档节点不再注入：已勾选完成的节点不会继续作为当前节点发送给 AI。
- 阶段边界保持不变：本阶段全部归档后停在该阶段末节点，不自动跨阶段，仍由阶段面板显式切换。

---

## 2026-08-08

- 分支事实排序修复：修复偏差分析「当前分支事实」记录楼层乱序的问题（例如出现 25 → 29 → 29 → 27）。此前列表顺序完全等于 AI 返回的数组顺序，而 AI 并不保证按楼层递增输出；同时上一轮保留下来的旧事实会被追加到列表末尾。现在事实统一按面板上显示的楼层从小到大排列，同一楼层保持原有先后，没有楼层信息的排在最后。
- 改写过的事实按更新层落位：显示「更新于第 40 层」的事实排到第 40 层的位置，而不是留在原来的记录层，保证列表右侧的楼层数字始终递增。
- 顺序同步到注入内容：面板显示、注入给写作模型的事实清单和回传给分析模型的上下文使用同一套顺序，不再只是界面上看着整齐。

---

## 2026-08-09

- 预览位置刷新恢复修复：修复刷新页面后状态栏预览节点回退到本阶段第一个节点的问题。此前从弹窗上下箭头切换预览只改内存不落盘，刷新后恢复的是上次切换阶段时写入的阶段首节点；现在弹窗预览与状态栏卡片点击一样即时保存，刷新后精确恢复到退出前停留的节点（含已归档节点）。
- 已归档节点不再被持续注入：修复停在已归档节点时持续提示词仍发送该节点内容的问题。持续提示词改为与其他注入路径一样按归档进度推导，始终发送当前阶段内第一个未归档节点——顺序归档时即最后一个已归档节点的后一个；整阶段全部归档后仍停在阶段末节点，不自动跨阶段。
- 数据未就绪防护：节点数据尚未加载完成的窗口期不再清空刚从存档恢复的预览位置，避免偶发的预览丢失。

---

## 2026-08-13

- 自动续跑清洗：清洗设置里新增「自动续跑清洗」开关（默认关闭）。开启后，一轮清洗结束时如果还有失败或未完成的分段，会自动重新发起续跑，直到全部补齐为止，不必守在界面前手动点「续跑清洗」。已完成的分段不会重复请求；上一轮标记失败的分段会重新排队。每轮之间按 3 秒起、逐轮递增、最多 30 秒的间隔等待，避免 Key 填错时高速空转。续跑期间标题栏会显示当前轮次，「暂停」按钮全程可用（包括轮间等待时），是随时中断的出口。
- 清洗完成提醒：开启自动续跑后，全部分段清洗完成时弹窗提示总段数、自动续跑轮次和总耗时。中途暂停不弹窗；未开启该开关时行为与此前完全一致。
- 穿书模式只注入当前节点：开启穿书模式时，阶段页的「未向量注入」不再把整个当前阶段发出去，改为只注入当前正在进行的那个节点——选「剧情节点」发该节点本身，选「压缩原文」发该节点所在的那一段清洗原文。此前整阶段注入会把本阶段中尚未演到的后续节点提前透露给模型。未开启穿书模式时没有进度概念，仍然按原样全量注入所有已开启的未向量阶段。
- 前情召回修复：修复「原著前情片段」几乎从不出现的问题。此前前情片段与当前阶段片段共用同一条相似度及格线，而前情讲的是几十章前的内容，与当前对话的相似度天然偏低，基本都被这条线挡掉，向量召回可以召回前情的优势实际上没有生效。现在前情使用一条更宽松的及格线（当前设定值的 0.6 倍），会随「向量相似度阈值」一起浮动，当前阶段片段的判定标准保持不变。
- 未开启穿书模式时也能划出前情：未开启穿书模式时插件无法直接得知正在演哪一段，此前会把所有已开启阶段一并当作「当前」，全书共抢 3 个召回名额，前情区块始终为空。现在改为根据本轮召回命中的分布反推当前所处阶段，更早的阶段归入前情，召回名额变为当前 3 条加前情 2 条。该推断只使用已缓存的向量数据在本地完成，不会额外调用接口；推断不出结果时按原有方式召回。
- 穿书模式下已关闭的阶段仍会作为前情被召回，这是原有设计，本次未作改动。

---

## 2026-08-15

- 用户代入姓名重复修复：修复开启「用户代入角色」后角色姓名越滚越长的问题——「凌小东」先变成「凌凌小东」，再变成「凌凌凌凌小东」，关掉该开关就恢复正常。原因是插件为了识别角色出场，会给三字以上、姓氏在百家姓内的中文名额外派生一个去姓短名（「凌小东」→「小东」），这个短名同时进了代入替换名单；当用户在酒馆里的角色名就是所扮演的原著角色本人时，替换目标本身就含有「小东」，把「小东」替换成「凌小东」等于在原名前多插一个姓。替换结果又会被写回聊天记录并保存，因此每来一轮就多叠一个字。现在替换目标自身已经包含的名字不再参与替换，并且所有名字改为一次扫描完成，刚写进去的内容不会在同一次替换里被再次命中。
- 历史楼层自动收敛：已经写进存档的「凌凌小东」「凌凌凌凌小东」会在下次注入或渲染时自动折回「凌小东」，不需要手动去改旧楼层。折叠只针对「重复的姓前缀 + 去姓短名」这一种形态，正常文本里的叠词不受影响。
- 穿书节点面板「进行中」修复：修复状态栏「选择节点」面板里标为「进行中」的节点与后台真正注入的节点不一致的问题。此前该面板显示的是阶段锚点，而注入节点会随勾选归档自动前移，勾过几个节点后两者就对不上了。现在面板的「进行中」与实际注入的节点始终是同一个；整个阶段全部归档后停在阶段末节点，该节点不再同时出现在「已归档」列表里。
- 自动续跑清洗开关样式统一：「自动续跑清洗」此前用的是浏览器原生方框勾选，与插件内其他开关不一致，现在改为与「流式输出」相同的文字「开／关」按钮。

---

## 2026-09-07

- 小说拖拽导入：上传区拦截拖拽事件，避免 TXT / MOBI 成功加载后又触发酒馆角色卡导入器的“不支持的文件类型”误报。
- 清洗请求结构：统一使用全局提示词来源，移除单独的清洗模式及分流代码；内置全局提示词与清洗规则作为独立消息发送，保留全局位置、深度和角色设置，并可分别预览。保留用户保存的清洗规则，明确区分规则与小说资料，默认提示词改为客观摘要与结构化整理。
- 全局预设选择：可指定插件使用的已保存酒馆预设，也可跟随前台当前预设；独立选择不切换前台 RP 预设，不覆盖插件 API 和模型。预设缺失时明确提示重新选择，列表支持刷新。
- 酒馆预设清洗：保留预设角色、顺序和深度，将本段资料与执行指令放入临时 `chatHistory`；只有预设条目展开宏，避免小说对白和标签被改写。临时历史不写入实际聊天；缺少历史槽位时在预设末尾补入并给出说明。
- 请求预览与诊断：清洗区域的“请求结构预览和失败响应记录”及分段列表支持查看实际消息与失败响应，标明全局来源及预设名称；预览按钮按内容定宽。仅在页面内存保留最近 10 次请求，响应限长并隐藏凭据。重试复用同一份请求，修复全局提示词重复叠加，并统一单段与自动清洗的前段参考。
- 清洗失败提示：使用酒馆原生通知显示失败段号、尝试次数、具体原因和处理建议，区分 429 限流、524 等网关超时、JSON 语法或结构异常、空响应与输出截断；酒馆未提供原始状态码时明确标注，主动暂停和跳过不再与超时混淆。
- 重新清洗确认：清空当前结果、剧情、角色、阶段划分与向量前先弹出酒馆确认框，取消后保留原进度；续跑仍直接补齐未完成分段。
- 使用说明：精简 README，前置已有的上下文注入位置、深度和消息角色设置入口，保留常见问题、报错处理与备份说明，并按现有代码修正穿书注入范围等描述。

---

## 当前状态摘要（截至 2026-09-07）

- 小说处理：支持 TXT 与部分 MOBI 导入、断点清洗、分段并发、可选自动续跑与完成提醒、剧情与角色提取、世界设定和文风分析。
- 大型内容管理：剧情、角色、阶段、穿书列表和小说库均使用轻量分页，并通过服务端重数据文件和可选自动保存保护长篇工作区。
- 剧情注入：未向量阶段可在 Token 上限内直接注入剧情节点或压缩原文；穿书模式下收紧为只注入当前进行中的节点。已向量阶段支持批量、限速、缓存、诊断、缺失补全，以及使用独立宽松阈值的原著前情召回。
- 穿书体验：支持节点进度、预览与推进分离、暂停、归纳、沉浸、未来阶段隔离、节点感知召回和后文行动建议。
- 角色与偏差：支持用户代入、角色别名、按聊天保存的 AI 实时人设，以及带真实来源楼层和变更历史的结构化分支事实。
- 备份与外观：支持 ZIP 和旧 JSON 导入导出、向量隐私保护、完整主题编辑、主题预设、状态栏跟随和移动端长内容查看。
